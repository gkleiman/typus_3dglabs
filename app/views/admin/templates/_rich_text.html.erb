<%
  custom = { :width => '680', :height => '200', :toolbar => 'Basic', :disabled => attribute_disabled?(attribute)) }
  options = options.merge!(custom)
%>

<li>
  <%= form.label attribute, label_text %>
  <%= fckeditor_textarea(:item, attribute, options) %>
</li>
